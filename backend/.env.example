# OpenRouter API Key (get from https://openrouter.ai/keys)
# This key provides access to ALL models (Claude, GPT, Gemini, Grok, etc.)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Legacy: Keep GEMINI_API_KEY for backward compatibility (can use same OpenRouter key)
# If you have a direct Gemini API key, you can use it here (from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your-openrouter-api-key-here

# Default model (fallback when model routing isn't specified)
# This is rarely used now that we have tier-based routing in backend/config/models.js
VERTEX_AI_MODEL=gemini-2.0-flash-001

# Server port
PORT=3001
